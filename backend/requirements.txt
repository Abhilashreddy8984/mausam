# Mausam Backend — Python dependencies
# Install with:  pip install -r requirements.txt
#
# Versions chosen for Python 3.13 compatibility (pre-built wheels available).

# Web framework
fastapi==0.115.12

# ASGI server (runs FastAPI)
uvicorn[standard]==0.34.3

# Data validation — FastAPI depends on this
pydantic==2.11.4
